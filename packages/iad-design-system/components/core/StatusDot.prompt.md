A small filled dot encoding state, optionally with a label. For reachability, probe status, and legends.

```jsx
<StatusDot tone="success" label="Reachable" />
<StatusDot tone="accent" pulse label="Scanning…" />
```

Tones map to semantic tokens. `pulse` adds a calm breathing ring — reserve it for genuinely live/active states.
